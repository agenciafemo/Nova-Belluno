{
  "name": "nova-belluno-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "dev": "astro dev",
    "sync:blog": "node scripts/sync-blog.mjs",
    "prebuild": "npm run sync:blog",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "audit:build": "node scripts/audit-build.mjs",
    "verify": "npm run check && npm run build && npm run audit:build"
  },
  "dependencies": {
    "@astrojs/sitemap": "3.7.3",
    "@supabase/supabase-js": "2.115.0",
    "astro": "7.2.4"
  },
  "devDependencies": {
    "@astrojs/check": "0.9.10",
    "typescript": "6.0.3"
  }
}
